/// A placeholder class that represents an entity or model.
class Item {
  const Item(this.id, this.nome, this.rating, this.valorMedio);

  final int id;
  final String nome;
  final double rating;
  final String valorMedio;
}
